# coding=utf-8

# Exercício 1:
# Mariazinha recebeu duas listas de valores numéricos do professor Roberto.
# Como Mariazinha é um exemplo de dedicação, ela quis testar um novo tipo
# de técnica: Mariazinha quer percorrer duas listas e adicionar seus valores
# em outro tipo de estrutura de forma que não se repitam quaisquer valores.
def build_set_from_lists(list1, list2):
    """
    TODO
     Percorrer ambas as listas entregues, olhar cada elemento e retornar
     uma estrutura que contenha TODOS os elementos das DUAS LISTAS SEM QUE
     QUAISQUER UM DELES SE REPITA.

    Ex1:
        A = [0, 1, 2, 3, 4]
        B = [0, 2, 4, 6, 8]
        RESULTADO = [0, 1, 2, 3, 4, 6, 8]


    Ex2:
        A = [0, 0, 0, 0, 1]
        B = [1, 2, 3, 4, 5]
        RESULTADO = [0, 1, 2, 3, 4, 5]
    """
    if not isinstance(list1, list) and not isinstance(list2, list):
        return
    else:
        return
